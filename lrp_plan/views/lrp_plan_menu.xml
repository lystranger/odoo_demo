<odoo>
  <data>

    <!-- client action -->
    <record id="lrp_plan.action_window" model="ir.actions.client">
      <field name="name">LRP Plan</field>
      <field name="tag">lrp_plan</field>
    </record>

    <!-- Top menu item -->
    <menuitem name="Lrp计划" id="lrp_plan.menu_root"/>

    <!-- actions -->
    <menuitem name="计划" 
        id="lrp_plan.lrp_plan" 
        parent="lrp_plan.menu_root"
        action="lrp_plan.action_window"/>

  </data>
</odoo>
